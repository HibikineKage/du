#include "mydef.h"

void printData (int data[WMAX][HMAX]) {
    int x, y;

    for (y = 0; y != HMAX; ++y) {
        for (x = 0; x != WMAX; ++x) {
            switch (data[x][y]) {
                case WHITE:

            }
        }
    }
}
